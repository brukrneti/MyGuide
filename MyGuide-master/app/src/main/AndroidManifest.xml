<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hr.foi.myguide">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/my_guide_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Prijava"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PocetnaStranica" />
        <activity android:name=".Registracija" />
        <activity
            android:name=".AddTour"
            android:label="Tours"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".MyTour"
            android:label="@string/title_activity_edit_tour"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".TourDetails"
            android:label="@string/title_activity_tour_details"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>